<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
    <form action="/update/<%= post._id %>" method="post" class="flex flex-col gap-4 max-w-sm mx-auto mt-10">
        <textarea name="content" id="" placeholder="What is in your mind"  class="w-full rounded"><%= post.content %></textarea>
        <button type="submit" class="bg-yellow-700 ">Update </button>
    </form>

</body>
</html>